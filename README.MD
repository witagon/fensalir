Installation Instructions
=========================
Run the bootstrap script found in the root of the repo (**$** is used for
illustrating the shell prompt)

    $ ./bootstrap --instructions

*or*

    $ ./bootstrap --i

For further information please use `--help` *or* `-h` option instead.
